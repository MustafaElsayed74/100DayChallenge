import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.habitat',
  appName: '100DayHabitat',
  webDir: 'dist/ClientApp'
};

export default config;
